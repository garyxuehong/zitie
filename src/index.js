import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import AppContainer from './App';

ReactDOM.render(
  <React.StrictMode>
    <AppContainer />
  </React.StrictMode>,
  document.body
);
